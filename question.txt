Servlet访问方式的声明：
   <servlet-mapping>
      <servlet-name>hello</servlet-name>
      <url-pattern>/hello</url-pattern>
   </servlet-mapping>
其中，<servlet-name>和servlet声明中的用法相同，并且应该与servlet声明中的名字保持一致。
	<url-pattern>表示访问方式，决定了在客户端如何访问这个Servlet。
Q:url-pattern具体是什么意思？什么叫决定了客户端如何访问这个servlet，编写了一个servlet如何确定它的控制范围？
A:就是用url-pattern控制的啊，上面的表述有问题，不好
Q:关于Servlet的名字？现在有三个，在src目录下包里面的java类的名字，在web.xml里声明的名字，在jsp文件访问的名字，怎么统一和区别
A:jsp访问的名称是在web.xml里声明的名称，在web.xml声明servlet-name时配置对应的servlet-class，就是java的包名+类名


request.getSession().setAttribute（）
Q：这个getSession()是得到了什么？
A:就是得到session啊，有4个数据银行，范围从大到小，application,session,request,page


Q：HttpServletRuqest,HttpServletResponse与ServletRequest,ServletResponse有什么异同？










